SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-key
SECRET_KEY=change-me-to-random-string
ADMIN_KEY=your-secret-admin-key
FRED_API_KEY=your-fred-api-key

# Free FRED API key: https://fred.stlouisfed.org/docs/api/api_key.html (instant, no cost)
# CBOE sentiment (SKEW, VIX3M, VIX9D, VIX6M) uses Yahoo Finance — no key needed

# Supabase SQL to run once:
# CREATE TABLE subscribers (
#   id uuid primary key default gen_random_uuid(),
#   invite_code text unique not null,
#   email text,
#   tier text default 'basic',
#   active boolean default true,
#   created_at timestamptz default now()
# );
#
# CREATE TABLE market_snapshots (
#   id uuid primary key default gen_random_uuid(),
#   created_at timestamptz default now(),
#   data jsonb not null
# );
#
# CREATE TABLE chain_snapshots (
#   id uuid primary key default gen_random_uuid(),
#   ticker text not null,
#   expiry text not null,
#   created_at timestamptz default now(),
#   data jsonb not null,
#   unique(ticker, expiry)
# );
#
# CREATE TABLE mega_snapshots (
#   id uuid primary key default gen_random_uuid(),
#   created_at timestamptz default now(),
#   data jsonb not null
# );
